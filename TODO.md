# TODO

## Local Development Setup (macOS)

- [ ] Install pre-commit: `brew install pre-commit && pre-commit install`

## Cluster Runtime

- [x] Evaluate migrating from k3d to [kind](https://kind.sigs.k8s.io/) — Done: both runtimes supported via `bootstrap.sh` (k3d) and `bootstrap-kind.sh` (kind)
